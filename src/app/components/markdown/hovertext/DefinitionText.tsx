import React from 'react';
import { PropsWithChildren } from 'react';

interface DefinitionText extends PropsWithChildren{
    
}

const DefinitionText = ({children}:DefinitionText) => {
    return (
        <div>
            
        </div>
    );
}

export default DefinitionText;
